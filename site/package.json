{
	"name": "fikeya-site",
	"private": true,
	"type": "module",
	"version": "0.0.0",
	"scripts": {
		"build": "node build.mjs",
		"test": "node validate.mjs && node --test release-site.test.ts",
		"deploy": "npm run build && node deployment-check.ts && wrangler pages deploy dist --project-name fikeya-site",
		"deploy:worker": "npm run build && node deployment-check.ts && wrangler deploy --config wrangler.worker.jsonc"
	},
	"devDependencies": {
		"@fontsource/ibm-plex-mono": "^5.3.0",
		"@fontsource/ibm-plex-sans": "^5.3.0",
		"marked": "18.0.11",
		"wrangler": "^4.83.0"
	}
}
