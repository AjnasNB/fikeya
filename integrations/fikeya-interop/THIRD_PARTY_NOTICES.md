# Third-party notices

The interoperability gateway uses these protocol SDKs without copying their implementations into Fikeya:

- `agent-client-protocol` 0.12.1, Apache License 2.0, maintained by the verified Agent Client Protocol organization.
- `mcp` 2.0.0, MIT License, maintained by the Model Context Protocol project.

Their transitive dependencies and exact hashes are recorded in `uv.lock`. Their names remain visible here because open-source
attribution is a release requirement; they are not represented as Fikeya-owned code.
